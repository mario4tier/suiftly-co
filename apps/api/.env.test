# Test Environment Configuration
# For running token refresh tests with short expiry times
# DO NOT use in production!

NODE_ENV=test
ENABLE_SHORT_JWT_EXPIRY=true
JWT_SECRET=TEST_SECRET_FOR_RAPID_TESTING

# Database (use test database)
DATABASE_URL=postgresql://deploy:deploy_password_change_me@localhost/suiftly_test

# Mock auth (skip signature verification)
MOCK_AUTH=true
