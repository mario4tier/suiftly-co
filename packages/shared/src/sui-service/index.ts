/**
 * Sui Service Types
 *
 * Shared types for blockchain escrow operations.
 */

export type {
  ISuiService,
  EscrowAccount,
  TransactionResult,
  DepositParams,
  WithdrawParams,
  ChargeParams,
  UpdateSpendingLimitParams,
  TransactionHistoryEntry,
  SpendingLimitCheck,
} from './types';
